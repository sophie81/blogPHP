!function (n) {
    n(function () {
        var i = null,
            t = n("#confirm"),
            e = null;
        n("input[name=delete]")
        .on("click", function (o) {
            o.preventDefault(),
            e = n(this).prev("input[name=title_h]").val(),
            t.dialog("open"),
            t.find("span").append(e),
            i = n(this).parent("form")
        }),
        t.dialog({
            autoOpen: !1,
            modal: !0,
            buttons: {
                Confirm: function (n) {
                    t.dialog("close"), t.find("span").empty(), i.submit()
                },
                Cancel: function () {
                    n(this).dialog("close"), t.find("span").empty()
                }
            }
        })
    }
)}
(jQuery);